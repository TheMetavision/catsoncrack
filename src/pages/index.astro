---
import Layout from '../layouts/Layout.astro';
import { tvSeriesSchema } from '../lib/schema';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import SectionDivider from '../components/SectionDivider.astro';
---

<Layout title="Cats On Crack - The Wildest Alley Cats You'll Ever Meet" jsonLd={tvSeriesSchema()}>
	<Navigation />

	<main>
		<section class="hero">
			<div class="hero-urban-bg"></div>
			<div class="hero-grain"></div>

			<div class="hero-split container">
				<div class="hero-image-col fade-left hero-anim-immediate">
					<div class="character-carousel" id="heroCarousel">

						<div class="carousel-slide active" data-index="0">
							<div class="character-frame">
								<img
									src="https://assets.zyrosite.com/cdn-cgi/image/format=auto,w=1024,h=1275,fit=crop/YanqXRJ3EDIXPJ8q/d700a221-efab-493b-94b1-5bc213c72b36-mp8qZRbbzEiL6R2D.png"
									alt="Toothless Terry"
									class="hero-character"
									loading="eager"
									fetchpriority="high"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Toothless Terry</div>
									<div class="nameplate-role">The Scheme Engine</div>
								</div>
								<div class="character-tagline">"Here's the thing about problems, pal — a problem is just a deal that hasn't been structured yet."</div>
							</div>
						</div>

						<div class="carousel-slide" data-index="1">
							<div class="character-frame">
								<img
									src="https://cdn.sanity.io/images/8ksun996/production/03c37b531e58887940cdecfcde357725bf1d93f7-1920x1080.png?w=896&h=1344&fit=crop&crop=center"
									alt="Whiskers McScruff"
									class="hero-character"
									loading="lazy"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Whiskers McScruff</div>
									<div class="nameplate-role">The Leader</div>
								</div>
								<div class="character-tagline">"Chaos is the cure. I've said it before. I've said it to the bins. The bins agree with me."</div>
							</div>
						</div>

						<div class="carousel-slide" data-index="2">
							<div class="character-frame">
								<img
									src="https://cdn.sanity.io/images/8ksun996/production/87fee4d99ed54660972e1f7253091a621f4d4d24-896x1344.png"
									alt="Luna the Loner"
									class="hero-character"
									loading="lazy"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Luna the Loner</div>
									<div class="nameplate-role">The Ghost</div>
								</div>
								<div class="character-tagline">"I don't have opinions about your plan. I have information."</div>
							</div>
						</div>

						<div class="carousel-slide" data-index="3">
							<div class="character-frame">
								<img
									src="https://cdn.sanity.io/images/8ksun996/production/e1ed30a067bbe4cff4f6b4962859557dd7fa103a-896x1344.png"
									alt="Chubby Cheeks"
									class="hero-character"
									loading="lazy"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Chubby Cheeks</div>
									<div class="nameplate-role">The Muscle</div>
								</div>
								<div class="character-tagline">"You don't just find snacks. You earn them. And by 'earn,' I mean you take them from the weak."</div>
							</div>
						</div>

						<div class="carousel-slide" data-index="4">
							<div class="character-frame">
								<img
									src="https://cdn.sanity.io/images/8ksun996/production/2058d4f4b5dff10ada3007a9c58828708cf2d685-896x1344.png"
									alt="Gizmo the Genius"
									class="hero-character"
									loading="lazy"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Gizmo the Genius</div>
									<div class="nameplate-role">The Inventor</div>
								</div>
								<div class="character-tagline">"Now, the beautiful thing about this is — oh, that's interesting. Actually, everyone move."</div>
							</div>
						</div>

						<div class="carousel-slide" data-index="5">
							<div class="character-frame">
								<img
									src="https://cdn.sanity.io/images/8ksun996/production/e323f904272aa43242549c2743913bdc24cb755c-896x1344.png"
									alt="Trixie the Trickster"
									class="hero-character"
									loading="lazy"
								/>
								<div class="character-ground-vignette"></div>
								<div class="character-nameplate">
									<div class="nameplate-name">Trixie the Trickster</div>
									<div class="nameplate-role">The Artist</div>
								</div>
								<div class="character-tagline">"I don't make mistakes. I make unexpectedly vivid decisions."</div>
							</div>
						</div>

						<button class="carousel-arrow carousel-prev" id="carouselPrev" aria-label="Previous character">&#8592;</button>
						<button class="carousel-arrow carousel-next" id="carouselNext" aria-label="Next character">&#8594;</button>

						<div class="carousel-dots" id="carouselDots">
							<button class="carousel-dot active" data-dot="0" aria-label="Slide 1"></button>
							<button class="carousel-dot" data-dot="1" aria-label="Slide 2"></button>
							<button class="carousel-dot" data-dot="2" aria-label="Slide 3"></button>
							<button class="carousel-dot" data-dot="3" aria-label="Slide 4"></button>
							<button class="carousel-dot" data-dot="4" aria-label="Slide 5"></button>
							<button class="carousel-dot" data-dot="5" aria-label="Slide 6"></button>
						</div>
					</div>
				</div>

				<div class="hero-text-col fade-right hero-anim-immediate">
					<div class="eyebrow">Now Streaming</div>
					<h1 class="main-title">The Wildest Alley Cats<br>You'll Ever Meet</h1>
					<p class="hero-body">A crew of street-savvy misfit cats. Rival gangs. Dark alleys. Glowing pipes. This is <strong>Cats On Crack</strong> — and they don't play by anyone's rules.</p>
					<div class="hero-ctas">
						<a href="https://www.youtube.com/shorts/ay215yKvntY" target="_blank" rel="noopener" class="cta-button primary-cta">
							<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="18" height="18"><path d="M8 5v14l11-7z"/></svg>
							Watch Our Trailer
						</a>
						<a href="/the-alley-squad" class="cta-button secondary-cta">
							Meet the Squad
							<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="16" height="16"><path d="M12 4l-1.41 1.41L16.17 11H4v2h12.17l-5.58 5.59L12 20l8-8z"/></svg>
						</a>
					</div>
					<div class="hero-stats stagger-children">
						<div class="stat">
							<span class="stat-number" data-count="5" data-suffix="+">5+</span>
							<span class="stat-label">Characters</span>
						</div>
						<div class="stat-divider"></div>
						<div class="stat">
							<span class="stat-number">Series 1</span>
							<span class="stat-label">Now Live</span>
						</div>
						<div class="stat-divider"></div>
						<div class="stat">
							<span class="stat-number" data-count="100" data-suffix="%">100%</span>
							<span class="stat-label">Unfiltered</span>
						</div>
					</div>
				</div>
			</div>

			<div class="hero-bottom-fade"></div>
		</section>

		<SectionDivider variant="crack" />

		<section id="about" class="section universe-section">
			<div class="container">
				<div class="universe-header fade-up">
					<p class="universe-eyebrow">Explore</p>
					<h2 class="universe-heading">The Cats On Crack Universe</h2>
				</div>
				<div class="pillars fade-up">

					<div class="pillar">
						<div class="pillar-icon-wrap">
							<svg class="pillar-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
								<path d="M8 5v14l11-7z"/>
							</svg>
						</div>
						<div class="pillar-label">Watch</div>
						<h3 class="pillar-headline">Watch the Series</h3>
						<p class="pillar-copy">The animated series is live on YouTube. New episodes dropping — subscribe so you don't miss a thing.</p>
						<a href="https://www.youtube.com/@CatsOnCrackMedia" target="_blank" rel="noopener noreferrer" class="pillar-cta">Watch Now &rarr;</a>
					</div>

					<div class="pillar-divider"></div>

					<div class="pillar">
						<div class="pillar-icon-wrap">
							<svg class="pillar-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
								<path d="M21 5c-1.11-.35-2.33-.5-3.5-.5-1.95 0-4.05.4-5.5 1.5-1.45-1.1-3.55-1.5-5.5-1.5S2.45 4.9 1 6v14.65c0 .25.25.5.5.5.1 0 .15-.05.25-.05C3.1 20.45 5.05 20 6.5 20c1.95 0 4.05.4 5.5 1.5 1.35-.85 3.8-1.5 5.5-1.5 1.65 0 3.35.3 4.75 1.05.1.05.15.05.25.05.25 0 .5-.25.5-.5V6c-.6-.45-1.25-.75-2-1zm0 13.5c-1.1-.35-2.3-.5-3.5-.5-1.7 0-4.15.65-5.5 1.5V8c1.35-.85 3.8-1.5 5.5-1.5 1.2 0 2.4.15 3.5.5v11.5z"/>
							</svg>
						</div>
						<div class="pillar-label">Read</div>
						<h3 class="pillar-headline">Read the Comics</h3>
						<p class="pillar-copy">Multi-panel. Gritty. Unfiltered. The Cats On Crack comic series — issues coming soon.</p>
						<a href="/alleyway-gazette" class="pillar-cta">Explore the Gazette &rarr;</a>
					</div>

					<div class="pillar-divider"></div>

					<div class="pillar">
						<div class="pillar-icon-wrap">
							<svg class="pillar-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
								<path d="M21.41 11.58l-9-9C12.05 2.22 11.55 2 11 2H4c-1.1 0-2 .9-2 2v7c0 .55.22 1.05.59 1.42l9 9c.36.36.86.58 1.41.58.55 0 1.05-.22 1.41-.59l7-7c.37-.36.59-.86.59-1.41 0-.55-.23-1.06-.59-1.42zM5.5 7C4.67 7 4 6.33 4 5.5S4.67 4 5.5 4 7 4.67 7 5.5 6.33 7 5.5 7z"/>
							</svg>
						</div>
						<div class="pillar-label">Rep</div>
						<h3 class="pillar-headline">Rep the Alley</h3>
						<p class="pillar-copy">Character apparel, prints, collectibles. The official COC merch store is dropping soon — get on the list.</p>
						<a href="/merch" class="pillar-cta">See What's Coming &rarr;</a>
					</div>

				</div>
			</div>
		</section>

		<SectionDivider variant="spray" />

		<section id="books" class="section books-section">
			<div class="container">
				<div class="content-split">
					<div class="image-side fade-left">
						<div class="image-card">
							<img
								src="/images/coc-coming-soon.jpg"
								alt="Cats on Crack - Coming Soon"
							/>
						</div>
					</div>
					<div class="text-side fade-right">
						<h2 class="section-title glow-text">Chaos, Claws, and Crack-Pipe Capers – The Ultimate Alley Adventure!</h2>
						<p>Dive into the gritty, hilarious world of <em>Cats on Crack</em>! This action-packed book series follows a crew of unpredictable alley cats as they hustle, brawl, and outsmart their rivals in the city's darkest corners. Fast-paced, wild, and full of twists—once you start, you won't stop!</p>
						<a href="/alleyway-gazette" class="cta-button secondary">Check Out Our Series</a>
					</div>
				</div>
			</div>
		</section>

		<SectionDivider variant="crack" flip={true} />

		<section id="characters" class="section characters-section">
			<div class="container">
				<div class="content-split reverse">
					<div class="text-side fade-left">
						<h2 class="section-title glow-text">Meet the Squad – The Wildest Cats in Town!</h2>
						<p>Get to know the misfit crew that rules the alley! From cunning masterminds to chaos-loving rogues, each cat brings their own brand of madness. Meet the legends, learn their stories, and see who's clawing their way to the top!</p>
						<a href="/the-alley-squad" class="cta-button secondary">Meet The Gang</a>
					</div>
					<div class="image-side fade-right">
						<div class="image-card">
							<img
								src="/images/coc-characters.png"
								alt="Cats on Crack Characters"
							/>
						</div>
					</div>
				</div>
			</div>
		</section>

		<SectionDivider variant="spray" flip={true} />

		<section id="newsletter" class="section newsletter-section">
			<div class="container">
				<div class="newsletter-content fade-up">
					<div class="newsletter-image">
						<img
							src="/images/coc-pipes.jpg"
							alt="Cats on Crack Newsletter"
						/>
					</div>
					<div class="newsletter-text">
						<h2 class="glow-text">Exclusive Alley Secrets, Sneaky Schemes & Feline Mayhem Delivered Weekly!</h2>
						<p class="subtitle">Subscribe to our newsletter</p>
						<form class="newsletter-form" id="newsletterForm">
							<input
								type="email"
								placeholder="Enter your email address"
								required
								aria-label="Email address"
								class="email-input"
							/>
							<button type="submit" class="cta-button">Subscribe</button>
						</form>
						<div class="nl-confirm" id="nl-confirm" style="display:none;" role="status" aria-live="polite">
							<div class="nl-signal">
								<div class="nl-signal__badge" aria-hidden="true">
									<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#fff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
								</div>
								<div>
									<div class="nl-signal__label">You're in the alley</div>
									<div class="nl-signal__headline">Welcome to the crew, stray.</div>
									<div class="nl-signal__sub">Alley secrets, sneaky schemes, and feline mayhem — heading to your inbox.</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</section>
	</main>

	<Footer />
</Layout>

<style>
	/* Hero elements animate in on page load, not on scroll */
	.hero-anim-immediate {
		animation-fill-mode: both;
	}
	.hero-image-col.fade-left.hero-anim-immediate {
		animation: heroSlideLeft 0.9s cubic-bezier(0.22,1,0.36,1) 0.1s both;
	}
	.hero-text-col.fade-right.hero-anim-immediate {
		animation: heroSlideRight 0.9s cubic-bezier(0.22,1,0.36,1) 0.25s both;
	}
	@keyframes heroSlideLeft {
		from { opacity: 0; transform: translateX(-48px); }
		to   { opacity: 1; transform: translateX(0); }
	}
	@keyframes heroSlideRight {
		from { opacity: 0; transform: translateX(48px); }
		to   { opacity: 1; transform: translateX(0); }
	}

	.universe-section {
		background: #181b22;
	}

	.universe-header {
		text-align: center;
		margin-bottom: 60px;
	}

	.universe-eyebrow {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 0.82rem;
		letter-spacing: 5px;
		color: #ff00ff;
		text-transform: uppercase;
		margin-bottom: 10px;
		opacity: 0.85;
	}

	.universe-heading {
		font-family: 'Bebas Neue', sans-serif;
		font-size: clamp(2.4rem, 5vw, 4rem);
		color: #ffffff;
		letter-spacing: 3px;
		line-height: 1;
		text-shadow:
			3px 3px 0 rgba(255,0,255,0.3),
			0 0 40px rgba(255,0,255,0.15);
	}

	.pillars {
		display: grid;
		grid-template-columns: 1fr auto 1fr auto 1fr;
		align-items: stretch;
		gap: 0;
		border: 1px solid rgba(255,0,255,0.15);
		border-radius: 4px;
		overflow: hidden;
	}

	.pillar {
		padding: 52px 44px 48px;
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		background: rgba(255,255,255,0.015);
		transition: background 0.3s ease;
		position: relative;
	}

	.pillar::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, transparent, rgba(255,0,255,0.5), transparent);
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.pillar:hover {
		background: rgba(255,0,255,0.04);
	}

	.pillar:hover::before {
		opacity: 1;
	}

	.pillar-divider {
		width: 1px;
		background: linear-gradient(
			to bottom,
			transparent 0%,
			rgba(255,0,255,0.3) 20%,
			rgba(255,0,255,0.3) 80%,
			transparent 100%
		);
		flex-shrink: 0;
	}

	.pillar-icon-wrap {
		width: 64px;
		height: 64px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 50%;
		background: rgba(255,0,255,0.1);
		border: 1px solid rgba(255,0,255,0.25);
		margin-bottom: 20px;
		transition: background 0.3s ease, box-shadow 0.3s ease;
	}

	.pillar:hover .pillar-icon-wrap {
		background: rgba(255,0,255,0.18);
		box-shadow: 0 0 24px rgba(255,0,255,0.2);
	}

	.pillar-icon {
		width: 26px;
		height: 26px;
		color: #ff00ff;
	}

	.pillar-label {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 0.72rem;
		letter-spacing: 5px;
		color: rgba(255,0,255,0.65);
		text-transform: uppercase;
		margin-bottom: 14px;
	}

	.pillar-headline {
		font-family: 'Bebas Neue', sans-serif;
		font-size: clamp(1.6rem, 2.5vw, 2.1rem);
		letter-spacing: 2px;
		color: #ffffff;
		margin-bottom: 14px;
		line-height: 1;
	}

	.pillar-copy {
		font-size: 0.95rem;
		line-height: 1.7;
		color: rgba(200,195,190,0.7);
		margin-bottom: 28px;
		flex: 1;
	}

	.pillar-cta {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 0.9rem;
		letter-spacing: 2px;
		color: #ff00ff;
		text-decoration: none;
		border-bottom: 1px solid rgba(255,0,255,0.35);
		padding-bottom: 2px;
		transition: color 0.2s ease, border-color 0.2s ease, text-shadow 0.2s ease;
	}

	.pillar-cta:hover {
		color: #ffffff;
		border-color: rgba(255,255,255,0.5);
		text-shadow: 0 0 10px rgba(255,0,255,0.5);
	}
	.books-section {
		background: #181b22;
	}
	.characters-section {
		background: #181b22;
	}
	.newsletter-section {
		background: #181b22;
	}

	.hero {
		min-height: 100vh;
		position: relative;
		display: flex;
		align-items: center;
		overflow: hidden;
		padding-top: 130px;
		padding-bottom: 60px;
		background: #181b22;
	}

	.hero-urban-bg {
		position: absolute;
		inset: 0;
		background:
			radial-gradient(ellipse 80% 60% at 30% 50%, rgba(24, 20, 30, 0.7) 0%, transparent 60%),
			radial-gradient(ellipse 60% 80% at 70% 60%, rgba(10, 10, 24, 0.6) 0%, transparent 65%),
			linear-gradient(160deg, #141620 0%, #181b22 40%, #141620 100%);
		z-index: 0;
	}

	.hero-urban-bg::before {
		content: '';
		position: absolute;
		inset: 0;
		background-image:
			repeating-linear-gradient(
				0deg,
				transparent,
				transparent 2px,
				rgba(255, 255, 255, 0.008) 2px,
				rgba(255, 255, 255, 0.008) 4px
			),
			repeating-linear-gradient(
				90deg,
				transparent,
				transparent 40px,
				rgba(255, 255, 255, 0.004) 40px,
				rgba(255, 255, 255, 0.004) 41px
			);
	}

	.hero-urban-bg::after {
		content: '';
		position: absolute;
		inset: 0;
		background:
			radial-gradient(ellipse 40% 30% at 20% 20%, rgba(255, 0, 255, 0.05) 0%, transparent 70%),
			radial-gradient(ellipse 30% 40% at 80% 70%, rgba(255, 0, 255, 0.05) 0%, transparent 70%);
	}

	.hero-grain {
		position: absolute;
		inset: 0;
		z-index: 1;
		background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='grain'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23grain)' opacity='0.06'/%3E%3C/svg%3E");
		background-size: 220px 220px;
		opacity: 0.5;
		pointer-events: none;
		mix-blend-mode: overlay;
	}

	.hero-split {
		position: relative;
		z-index: 2;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 60px;
		align-items: flex-end;
	}

	.hero-image-col {
		position: relative;
		display: flex;
		justify-content: center;
	}

	/* ── CAROUSEL ── */
	.character-carousel {
		position: relative;
		width: 100%;
		display: flex;
		justify-content: center;
	}

	.carousel-slide {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		opacity: 0;
		transition: opacity 0.5s ease;
		pointer-events: none;
	}

	.carousel-slide.active {
		position: relative;
		opacity: 1;
		pointer-events: auto;
	}

	.character-frame {
		position: relative;
		display: flex;
		flex-direction: column;
		align-items: center;
	}

	.hero-character {
		position: relative;
		z-index: 2;
		width: 100%;
		max-width: 460px;
		height: auto;
		object-fit: contain;
		filter:
			drop-shadow(0 0 20px rgba(255, 0, 255, 0.3))
			drop-shadow(0 0 50px rgba(255, 0, 255, 0.12))
			drop-shadow(0 20px 40px rgba(0, 0, 0, 0.9));
	}

	.hero-character-svg {
		width: 100%;
		max-width: 460px;
		aspect-ratio: 0.8;
		display: flex;
		align-items: center;
		justify-content: center;
		filter: none;
	}

	.char-svg {
		width: 70%;
		height: 70%;
		opacity: 0.85;
	}

	.character-ground-vignette {
		position: absolute;
		bottom: 0;
		left: -10%;
		right: -10%;
		height: 45%;
		background: linear-gradient(
			to top,
			#181b22 0%,
			rgba(24, 27, 34, 0.85) 25%,
			rgba(24, 27, 34, 0.4) 55%,
			transparent 100%
		);
		z-index: 3;
		pointer-events: none;
	}

	.character-nameplate {
		position: absolute;
		bottom: 90px;
		left: 0;
		right: 0;
		z-index: 4;
		text-align: center;
	}

	.nameplate-name {
		font-family: 'Bebas Neue', 'Impact', sans-serif;
		font-size: clamp(2rem, 4vw, 3.2rem);
		letter-spacing: 4px;
		color: #fff;
		text-shadow:
			0 0 20px rgba(255, 0, 255, 0.7),
			0 0 40px rgba(255, 0, 255, 0.3),
			2px 2px 0 rgba(0, 0, 0, 0.9),
			-1px -1px 0 rgba(0, 0, 0, 0.9);
		line-height: 1;
		text-transform: uppercase;
	}

	.nameplate-role {
		display: inline-block;
		margin-top: 8px;
		padding: 4px 16px;
		background: rgba(255, 0, 255, 0.15);
		border: 1px solid rgba(255, 0, 255, 0.5);
		border-radius: 3px;
		font-family: 'Inter', sans-serif;
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 3px;
		text-transform: uppercase;
		color: #ff00ff;
		text-shadow: 0 0 10px rgba(255, 0, 255, 0.5);
	}

	.character-tagline {
		position: absolute;
		top: 30px;
		left: -20px;
		right: -20px;
		z-index: 4;
		font-family: 'Inter', sans-serif;
		font-size: 0.82rem;
		font-style: italic;
		color: #ff00ff;
		text-align: center;
		line-height: 1.6;
		letter-spacing: 0.3px;
		text-shadow: 0 0 12px rgba(255, 0, 255, 0.4), 0 1px 4px rgba(0, 0, 0, 0.9);
	}

	/* ── CAROUSEL CONTROLS ── */
	.carousel-arrow {
		position: absolute;
		top: 50%;
		transform: translateY(-50%);
		z-index: 10;
		background: rgba(255, 0, 255, 0.1);
		border: 1px solid rgba(255, 0, 255, 0.35);
		color: #ff00ff;
		width: 36px;
		height: 36px;
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 1.1rem;
		cursor: pointer;
		transition: background 0.2s ease, box-shadow 0.2s ease;
		border-radius: 3px;
	}

	.carousel-arrow:hover {
		background: rgba(255, 0, 255, 0.22);
		box-shadow: 0 0 12px rgba(255, 0, 255, 0.35);
	}

	.carousel-prev { left: 0; }
	.carousel-next { right: 0; }

	.carousel-dots {
		position: absolute;
		bottom: 60px;
		left: 0;
		right: 0;
		z-index: 10;
		display: flex;
		justify-content: center;
		gap: 8px;
	}

	.carousel-dot {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: rgba(255, 0, 255, 0.25);
		border: 1px solid rgba(255, 0, 255, 0.4);
		cursor: pointer;
		padding: 0;
		transition: background 0.2s ease, transform 0.2s ease;
	}

	.carousel-dot.active {
		background: #ff00ff;
		transform: scale(1.25);
		box-shadow: 0 0 6px rgba(255, 0, 255, 0.6);
	}

	.hero-text-col {
		display: flex;
		flex-direction: column;
		justify-content: center;
		padding-bottom: 30px;
	}

	.eyebrow {
		font-family: 'Inter', sans-serif;
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 4px;
		text-transform: uppercase;
		color: #ff00ff;
		margin-bottom: 20px;
		display: flex;
		align-items: center;
		gap: 10px;
	}

	.eyebrow::before {
		content: '';
		display: inline-block;
		width: 28px;
		height: 2px;
		background: #ff00ff;
		border-radius: 2px;
	}

	.main-title {
		font-size: clamp(2.6rem, 4.5vw, 4.2rem);
		margin-bottom: 24px;
		line-height: 1.05;
		color: #ffffff;
		text-shadow:
			0 0 30px rgba(255, 0, 255, 0.2),
			0 2px 8px rgba(0, 0, 0, 0.8);
		letter-spacing: 2px;
	}

	.hero-body {
		font-size: 1.1rem;
		line-height: 1.8;
		color: rgba(190, 185, 178, 0.85);
		margin-bottom: 36px;
		max-width: 480px;
	}

	.hero-body strong {
		color: #fff;
	}

	.hero-ctas {
		display: flex;
		gap: 16px;
		flex-wrap: wrap;
		margin-bottom: 48px;
	}

	.primary-cta {
		display: inline-flex;
		align-items: center;
		gap: 10px;
		padding: 16px 36px;
		background: #ff00ff;
		color: white;
		font-family: 'Bebas Neue', sans-serif;
		font-size: 1.15rem;
		letter-spacing: 2px;
		border-radius: 6px;
		text-decoration: none;
		box-shadow:
			0 4px 20px rgba(255, 0, 255, 0.4),
			0 0 40px rgba(255, 0, 255, 0.12);
		transition: all 0.3s ease;
		text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.4);
	}

	.primary-cta:hover {
		transform: translateY(-3px);
		box-shadow:
			0 8px 30px rgba(255, 0, 255, 0.55),
			0 0 60px rgba(255, 0, 255, 0.2);
		color: white;
		text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.4);
	}

	.secondary-cta {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		padding: 16px 32px;
		background: transparent;
		color: rgba(220, 215, 210, 0.9);
		font-family: 'Bebas Neue', sans-serif;
		font-size: 1.15rem;
		letter-spacing: 2px;
		border-radius: 6px;
		border: 1px solid rgba(255, 255, 255, 0.18);
		text-decoration: none;
		transition: all 0.3s ease;
	}

	.secondary-cta:hover {
		background: rgba(255, 255, 255, 0.06);
		border-color: rgba(255, 255, 255, 0.35);
		color: #fff;
		transform: translateY(-2px);
		text-shadow: none;
	}

	.hero-stats {
		display: flex;
		align-items: center;
		gap: 24px;
	}

	.stat {
		display: flex;
		flex-direction: column;
		gap: 2px;
	}

	.stat-number {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 1.6rem;
		letter-spacing: 1px;
		color: #fff;
		line-height: 1;
	}

	.stat-label {
		font-size: 0.68rem;
		font-weight: 600;
		letter-spacing: 2px;
		text-transform: uppercase;
		color: rgba(160, 155, 148, 0.7);
	}

	.stat-divider {
		width: 1px;
		height: 32px;
		background: rgba(255, 255, 255, 0.12);
	}

	.hero-bottom-fade {
		position: absolute;
		bottom: 0;
		left: 0;
		right: 0;
		height: 120px;
		background: linear-gradient(to top, #181b22 0%, transparent 100%);
		z-index: 3;
		pointer-events: none;
	}

	.description {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--text-secondary);
	}

	.description p {
		margin-bottom: 20px;
	}

	.description strong {
		color: var(--text-primary);
		font-weight: 700;
	}

	.highlight {
		padding-left: 20px;
		border-left: 4px solid var(--primary);
	}

	.tagline {
		font-size: 1.2rem;
		color: var(--text-primary);
		font-weight: 600;
		margin-top: 30px;
	}

	.cta-button {
		display: inline-block;
		padding: 16px 40px;
		background: var(--accent-gradient);
		color: white;
		font-size: 1.3rem;
		font-weight: 900;
		letter-spacing: 2px;
		border-radius: 8px;
		margin-top: 30px;
		text-transform: uppercase;
		box-shadow: 0 4px 20px rgba(255, 0, 255, 0.4);
		transition: all 0.3s ease;
		text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
	}

	.cta-button:hover {
		transform: translateY(-3px);
		box-shadow: var(--glow-strong);
		color: white;
	}

	.cta-button.secondary {
		background: transparent;
		border: 3px solid var(--primary);
		color: var(--primary);
	}

	.cta-button.secondary:hover {
		background: var(--primary);
		color: white;
		text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
	}

	.content-split {
		display: flex;
		flex-direction: column;
		gap: 40px;
		align-items: center;
		max-width: 900px;
		margin: 0 auto;
	}

	.content-split.reverse {
		direction: ltr;
	}

	.content-split.reverse > * {
		direction: ltr;
	}

	.content-split .image-side {
		width: 100%;
	}

	.content-split .text-side {
		text-align: center;
		width: 100%;
	}

	.section-title {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 25px;
		line-height: 1.2;
		color: var(--primary);
	}

	.text-side p {
		font-size: 1.1rem;
		line-height: 1.8;
		color: var(--text-secondary);
		margin-bottom: 30px;
	}

	.image-card {
		position: relative;
		border-radius: 12px;
		overflow: hidden;
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
		transition: transform 0.3s ease, box-shadow 0.3s ease;
		border: 2px solid rgba(255, 0, 255, 0.2);
	}

	.image-card:hover {
		transform: translateY(-10px);
		box-shadow: 0 20px 60px rgba(255, 0, 255, 0.4);
		border-color: var(--primary);
	}

	.image-card img {
		width: 100%;
		height: auto;
		display: block;
	}

	.newsletter-section {
		padding: 120px 0;
	}

	.newsletter-content {
		display: flex;
		flex-direction: column;
		gap: 40px;
		align-items: center;
		background: var(--bg-card);
		border-radius: 20px;
		padding: 0;
		box-shadow: 0 10px 50px rgba(0, 0, 0, 0.5);
		border: 2px solid rgba(255, 0, 255, 0.2);
		overflow: hidden;
		max-width: 900px;
		margin: 0 auto;
	}

	.newsletter-image {
		width: 100%;
		border-radius: 0;
		overflow: hidden;
	}

	.newsletter-image img {
		width: 100%;
		height: auto;
		display: block;
	}

	.newsletter-text {
		padding: 0 60px 60px;
		text-align: center;
	}

	.newsletter-text h2 {
		font-size: clamp(1.8rem, 3vw, 2.5rem);
		margin-bottom: 15px;
		color: var(--primary);
	}

	.subtitle {
		font-size: 1rem;
		color: var(--text-secondary);
		margin-bottom: 30px;
		text-transform: uppercase;
		letter-spacing: 2px;
	}

	.newsletter-form {
		display: flex;
		gap: 15px;
		flex-wrap: wrap;
	}

	.email-input {
		flex: 1;
		min-width: 250px;
		padding: 15px 20px;
		background: var(--bg-darker);
		border: 2px solid rgba(255, 0, 255, 0.3);
		border-radius: 8px;
		color: var(--text-primary);
		font-size: 1rem;
		transition: all 0.3s ease;
	}

	.email-input:focus {
		outline: none;
		border-color: var(--primary);
		box-shadow: 0 0 0 3px rgba(255, 0, 255, 0.1);
	}

	/* newsletter-image styles defined above with newsletter-content */

	/* Newsletter confirmation card */
	.nl-signal {
		display: flex;
		align-items: center;
		gap: 14px;
		background: var(--bg-darker, #0d0f14);
		border: 2px solid rgba(255, 0, 255, 0.2);
		border-left: 4px solid var(--primary, #ff00ff);
		border-radius: 12px;
		padding: 1.25rem 1.5rem;
		text-align: left;
		animation: nlSignalIn 0.4s ease-out;
	}

	@keyframes nlSignalIn {
		from { opacity: 0; transform: translateY(8px); }
		to { opacity: 1; transform: translateY(0); }
	}

	.nl-signal__badge {
		flex-shrink: 0;
		width: 38px;
		height: 38px;
		border-radius: 50%;
		background: var(--primary, #ff00ff);
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 0 15px rgba(255, 0, 255, 0.4);
	}

	.nl-signal__label {
		font-family: 'Bebas Neue', sans-serif;
		font-size: 0.625rem;
		color: var(--primary, #ff00ff);
		letter-spacing: 2.5px;
		margin-bottom: 3px;
		text-transform: uppercase;
		text-shadow: 0 0 8px rgba(255, 0, 255, 0.3);
	}

	.nl-signal__headline {
		font-family: 'Bebas Neue', sans-serif;
		color: #fff;
		font-size: 1.2rem;
		letter-spacing: 1.5px;
		line-height: 1.3;
		text-transform: uppercase;
		text-shadow: 2px 2px 0 rgba(255, 0, 255, 0.15);
	}

	.nl-signal__sub {
		font-family: 'Inter', sans-serif;
		color: var(--text-secondary, rgba(255,255,255,0.5));
		font-size: 0.8rem;
		margin-top: 3px;
	}

	@media (max-width: 1100px) {
		.hero-split {
			gap: 36px;
		}
	}

	@media (max-width: 900px) {
		.hero-split {
			grid-template-columns: 1fr;
			align-items: center;
			gap: 0;
		}

		.hero-image-col {
			order: 1;
		}

		.hero-text-col {
			order: 2;
			padding-bottom: 0;
			padding-top: 16px;
		}

		.hero-character {
			max-width: 320px;
			margin: 0 auto;
		}

		.hero-character-svg {
			max-width: 320px;
		}

		.character-nameplate {
			bottom: 60px;
		}

		.character-tagline {
			top: 16px;
		}
	}

	@media (max-width: 968px) {
		.pillars {
			grid-template-columns: 1fr;
		}

		.pillar-divider {
			width: auto;
			height: 1px;
			background: linear-gradient(
				to right,
				transparent 0%,
				rgba(255,0,255,0.3) 20%,
				rgba(255,0,255,0.3) 80%,
				transparent 100%
			);
		}

		.pillar {
			padding: 40px 32px;
		}

		.newsletter-text {
			padding: 0 30px 40px;
		}
	}

	@media (max-width: 768px) {
		.hero {
			padding-top: 110px;
			padding-bottom: 40px;
		}

		.hero-ctas {
			flex-direction: column;
		}

		.hero-ctas .primary-cta,
		.hero-ctas .secondary-cta {
			width: 100%;
			justify-content: center;
			text-align: center;
		}

		.hero-stats {
			gap: 16px;
		}

		.newsletter-content {
			padding: 40px 30px;
		}

		.newsletter-form {
			flex-direction: column;
		}

		.email-input {
			width: 100%;
		}
	}
</style>

<script>
	const form = document.getElementById('newsletterForm');
	const btn = form?.querySelector('button[type="submit"]') as HTMLButtonElement;
	const nlConfirm = document.getElementById('nl-confirm');

	form?.addEventListener('submit', async (e) => {
		e.preventDefault();
		const email = form.querySelector('input[type="email"]') as HTMLInputElement;
		if (!email?.value) return;

		const originalText = btn.textContent;
		btn.textContent = 'Subscribing...';
		btn.disabled = true;

		try {
			const res = await fetch('https://connect.mailerlite.com/api/subscribers', {
				method: 'POST',
				headers: {
					'Content-Type': 'application/json',
					'Authorization': 'Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9..D_zKiP5yLqx0XKrNeOt146s6arBj9oorzQFkOsmNHAjcpACKTlkEcckUDnaAlPbTZMJKJ2zJKacOmrjKUJURql5F6xako4CYgbLo5EqaMoDH5fznm_GtVDaWDcbm9QCBxgeeqYcamOfPnsvwPHUJThgKz2IZ4Y4Hc8H7IudylESYoktYoz91nBk4osPApnI5G0-PiTXaNvH0EdHdWl_0aNfnq7aLcg0bNjmL4a-xxxON6-t-ApS6EA6Wb87bVU568XlMk7AATxW800qHCEfU47YmoQ2jBLuLXWaFUYmzKgP4hhgzd8ku2GCERPUiJj90ZBevup-WSSgKCpIeRW0AZFmTW7bWRQlxuYzbw03Xw_l9MD__aK47JGLtSf7jZWTKBwrAv-gfe6brflahxjDmfNot0NfK8gYDnZkLOCHyDp8Ky7kNLZK8YCEl-jhQQOVBY_zVz3tu-omxMqHSE7PLX5_mDNt-Lslc6lkdk---Jg6hnLMTSfKnQRrC-awJ4ZYCgjtWUG2g1kQXpEQ8VXg6sdGwYKsf-64nMFQ05laakp-QOWr9vC52ZC1fr9Ctlykl2jUKJui2HY_akqSxjXgCY14apcGzmmMWEtNljC_A6qARuu6U5qcRDEmhCTew3yGrML8JMos4cbxbTTO5fndMbcYktjZ006PZasHlv4zyKX4'
				},
				body: JSON.stringify({
					email: email.value,
					groups: ['184360844895716414'],
					fields: { source: 'catsoncrack-homepage' }
				})
			});

			if (res.ok || res.status === 200 || res.status === 201) {
				form.style.display = 'none';
				if (nlConfirm) nlConfirm.style.display = 'block';
			} else if (res.status === 422) {
				form.style.display = 'none';
				if (nlConfirm) {
					const signal = nlConfirm.querySelector('.nl-signal');
					if (signal) {
						signal.querySelector('.nl-signal__label')!.textContent = 'Already in the alley';
						signal.querySelector('.nl-signal__headline')!.textContent = 'We know you, stray.';
						signal.querySelector('.nl-signal__sub')!.textContent = "You signed up before. The crew's already got you covered.";
					}
					nlConfirm.style.display = 'block';
				}
			} else {
				throw new Error('Subscription failed');
			}
		} catch (err) {
			btn.textContent = 'Something went wrong — try again';
			btn.disabled = false;
			setTimeout(() => { btn.textContent = originalText; }, 3000);
		}
	});

	(function() {
		const carousel = document.getElementById('heroCarousel');
		if (!carousel) return;

		const slides = carousel.querySelectorAll('.carousel-slide');
		const dots = carousel.querySelectorAll('.carousel-dot');
		const prevBtn = document.getElementById('carouselPrev');
		const nextBtn = document.getElementById('carouselNext');
		let current = 0;
		let timer: ReturnType<typeof setInterval>;

		function goTo(index: number) {
			slides[current].classList.remove('active');
			dots[current].classList.remove('active');
			current = (index + slides.length) % slides.length;
			slides[current].classList.add('active');
			dots[current].classList.add('active');
		}

		function startTimer() {
			timer = setInterval(() => goTo(current + 1), 4000);
		}

		function resetTimer() {
			clearInterval(timer);
			startTimer();
		}

		prevBtn?.addEventListener('click', () => { goTo(current - 1); resetTimer(); });
		nextBtn?.addEventListener('click', () => { goTo(current + 1); resetTimer(); });

		dots.forEach((dot) => {
			dot.addEventListener('click', () => {
				const idx = parseInt((dot as HTMLElement).dataset.dot || '0', 10);
				goTo(idx);
				resetTimer();
			});
		});

		startTimer();
	})();
</script>
